# 1. Environment and Secrets
# Never commit secrets, API keys, or database URLs
.env
*.env

# 2. Database Files (Local Development)
# The local SQLite DB. Production (Render) uses PostgreSQL.
conference.db
*.db
*.sqlite
identifier.sqlite

# 3. Virtual Environment
# Contains all your installed packages.
# requirements.txt is the list, not the folder itself.
.venv
venv

# 4. Local Utility Scripts
# These are one-off scripts, not part of the main app.
super_admin.py
create_admin.py
test.py

# 5. Python Cache
# Auto-generated by Python, as we discussed.
__pycache__/
*.pyc

# 6. IDE / Editor Files
# Project files for editors like PyCharm (.idea) or VSCode (.vscode)
.idea/
.vscode/